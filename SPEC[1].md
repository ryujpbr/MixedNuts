# Mixed Nuts — エンジン仕様書 / 実装契約

実装をAIに任せる前提で書かれた文書です。**この文書が曖昧な箇所は、必ず実装が壊れます。**
曖昧なまま書かせるのではなく、まずここを埋めてから投げてください。

---

## 0. 設計原則（絶対に曲げない4つ）

1. **サーバー権威型。** クライアントは描画と入力だけ。カードの決定、合法手の判定、ポットの計算はすべてサーバー。クライアントの申告を信じる箇所を1つでも作ったら終わりです。
2. **バリアントはデータ。** ゲーム追加はコード追加ではなく `variants/registry.ts` への設定追加。`if (variant === 'razz')` という分岐をエンジン内に書いたら設計が負けています。
3. **エンジンは純粋関数。** `packages/engine` の中で `Math.random()` / `Date.now()` / I/O を呼ばない。乱数と時刻は引数で注入。これがないとリプレイ検証ができません。
4. **1手ごとに完全なイベントログ。** ハンド履歴からリプレイして同じ結果になることをテストで保証する。バグ報告が「再現できません」で終わる地獄を避けます。

---

## 1. モノレポ構成

```
mixednuts/
├─ packages/
│  ├─ engine/          ← 本パッケージ。純粋。DB もソケットも知らない
│  ├─ protocol/        ← クライアント/サーバー共有の型とイベント定義（zod）
│  └─ ui/              ← React コンポーネント（テーブル描画）
└─ apps/
   ├─ server/          ← Node + WebSocket。プール、卓、永続化
   └─ web/             ← Vite + React。PWA
```

エンジンをクライアントからも import できることが重要です。合法アクションの事前判定、
ハンドリプレイ、将来の練習モードがすべて同じコードで動きます。

---

## 2. 実装済み（本パッケージ）

| ファイル | 内容 | 状態 |
|---|---|---|
| `src/cards.ts` | カード表現（0..51 の整数）、シャッフル、組み合わせ列挙 | 完成・テスト済 |
| `src/eval/core.ts` | **フラグ付き5枚ランカー1本**。high / 2-7 / A-5 をすべてここで処理 | 完成・テスト済 |
| `src/eval/index.ts` | 評価器レジストリ（high, low8, lowA5, low27, badugi）と使用枚数ルール | 完成・テスト済 |
| `src/variant.ts` | **バリアント宣言スキーマ**（中核設計物） | 完成 |
| `src/variants/registry.ts` | 17ゲームの設定。stable 13 / draft 4 / 未確定 1 | 完成 |
| `src/showdown.ts` | ポット分配（Hi-Lo分割、不成立時のスクープ、端数チップ） | 完成・テスト済 |
| `src/table.ts` | 席・ハンド状態・アクション・イベントログの型 | 完成 |
| `src/betting.ts` | **ベッティングステートマシン**（合法手計算、NL/PL/FL のサイズ、ショートオールイン） | 完成・テスト済 |
| `src/pot.ts` | **ポットマネージャ**（未マッチ返金、サイドポット構築） | 完成・テスト済 |
| `src/hand.ts` | ハンドランナー（フェーズ駆動、配札、精算、リプレイ） | フロップ系完成・テスト済 |

**稼働中のゲーム: NLH / PLO / PLO8 / BigO / FLH / FLO8**（フロップ系すべて）。
`npm test` で55ケース通過。乱択10万ハンドのソークでチップ保存則・デッキ整合性・非負スタックを検証済み。`npm run typecheck` クリーン。

### ベッティングの使い方

```ts
let s = startHand({ variant: getVariant('nlh'), config, seats, deck });
while (s.status === 'running') {
  const la = legalActions(s, variant)!;   // クライアントのボタンもこれで出す
  s = applyAction(s, variant, playerChoice);
}
s.result;   // awards / refunds / potTotal
```

`applyAction` は**新しい state を返す**（入力は不変）。ロールバックとリプレイがこれで成立します。

### スプリットポットの端数

ハイロー split の端数チップは **`variant.showdown` の先頭シェア（=ハイ側）** に行きます。同一シェア内で複数人がチョップした場合の端数は、ボタンの左から数えて最初の勝者。どちらも標準ルールです。クォーター（ローが2人タイ、ハイが1人）は 3/4 : 1/4 に分かれることをテストで固定しています。

### 返金ルール（乱択テストで見つけたバグ）

未マッチのチップ返金には**2ケース**あります。1つ目しか実装していない実装が世に多く、2つ目は静かにチップ保存則を壊します。

1. **コールされなかったベット**: 40しか持たない相手に100ベット→60が戻る。各席を「自分以外の最大コミット額」でキャップ。
2. **勝てるプレイヤーがいないサイドポット**: ショートスタックが2800でオールイン、深い2席がサイドポットを3168まで作り、**その2席が両方フォールド**。368ずつは誰も取れないので本人に返る。各席を「まだハンドに残っている席の最大コミット額」でキャップ。

2つ目は手書きのテストでは出ません。乱択プロパティテストで出ました。プレイヤーからの苦情から逆算してデバッグするのは不可能な種類のバグです。

### 評価器に手を入れるときの警告

ハンド評価のコピーを**絶対に増やさないでください**。`rank5()` 1本に集約されているため、
ここのバグ修正は13ゲーム同時に効きます。ここを2箇所に分岐させた瞬間、
「Razzだけ稀にポットが逆に行く」類のバグが永久に取れなくなります。

---

## 3. 未実装（次に作るもの・優先順）

### ~~3.1 ベッティングラウンドのステートマシン~~ → 実装済（§2）

```ts
interface BettingState {
  seats: SeatState[];          // stack, committedThisRound, folded, allIn
  currentBet: number;          // このラウンドの最大コミット額
  minRaiseTo: number;
  lastAggressor: number | null;
  actionOn: number;
  betsThisRound: number;       // FL のキャップ判定用
}
```

**合法手の計算は1関数に集約すること**（`legalActions(state, variant, phase): Action[]`）。
クライアントもこの関数を使ってボタンを出す。サーバーは受け取ったアクションを同じ関数で検証する。

ベットサイズの定義（ここを曖昧にすると必ず食い違います）:

- **No-Limit**: `raiseTo >= minRaiseTo`、上限は自分のスタック。
- **Pot-Limit**: `callAmount = currentBet - committed[seat]`、`maxRaiseTo = currentBet + (potTotal + callAmount)`。`potTotal` は**このラウンドの既存ベットを含む**総ポット。
- **Fixed-Limit**: ベット額は tier（small=1単位 / big=2単位）で固定。1ラウンドあたり `betsPerRound`（=4、ベット1+レイズ3）でキャップ。
  - **決定済み（ハウスルール）**: ヘッズアップでも4ベットキャップを維持する。`betsPerRound` を無条件に適用。**WSOP は残り2人になった時点でキャップを外す**ので、ここは意図的な差分。利用規約かヘルプに明記すること。
- **Stud の open pair**: `openPairDoubleBet` が立っている phase で、アップカードにペアが見えている席は big bet を選択できる。この席がbig betを選んだ場合、そのラウンドは以降 big bet 単位になる。

### ~~3.2 ポットマネージャ（サイドポット）~~ → 実装済（§2）

`showdown.ts` の `awardPot()` は**1つのポット**しか扱いません。意図的です。

サイドポット構築:
1. 各席の**ハンド累計**コミット額を集計。
2. オールイン額の昇順にレベルを作る。
3. レベルごとにポットを作り、そのレベル以上をコミットした席のみを eligible にする。
4. 各ポットについて eligible 席で `awardPot()` を呼ぶ。

**不変条件**: 全ポットの支払い合計 = 全席のコミット合計。テストで必ず assert すること。
`awardPot()` は既に内部で保存則チェックをしています。

### 3.3 ドロー処理とデッキ枯渇

`maxDiscards: 'all'` のフェーズで、各席は0〜手札枚数を交換。順番は `firstToAct` 準拠。

6-max × 5枚 × 3ドローで最悪 52枚を超えます。ルール:
`deckExhaustion: 'reshuffle-discards'` = 山が尽きたら、**その時点までの捨て札**（バーンカードを除く）をまとめてシャッフルし直して続行。捨て札の記録を必ず保持してください。

### 3.4 ファストフォールド・プール ★Mixed Nuts の心臓部

```
Pool (単一)
 ├─ waiting: Set<PlayerId>        // 次のハンドを待っている
 ├─ activeTables: Table[]          // 進行中の一時卓
 └─ tick(): 6人揃ったら卓を組んで配牌
```

- フォールド/ハンド終了 → 即 `waiting` に戻る。卓は使い捨て。
- **スタックは毎ハンド 100bb にリセット**することを強く推奨（Ten-Four と同じ）。理由: ディープ/ショートでプールを分けずに済み、席替えのスタック引き継ぎ問題が消え、統計が比較可能になる。深いスタックが欲しくなるのは人が増えてからです。
- **同一組み合わせ連続の回避**: プールが小さいと同じ6人が繰り返し当たります。直近N回の対戦相手に負の重みを付けたマッチングを最初から入れてください。後付けは面倒です。
- **共謀対策**: 同一IP / 同一デバイス / 同一支払い手段（なし）ではなく、**同時接続の相関**を見るしかありません。最低限、同一アカウントの多重接続拒否と、卓内の同時着席を防ぐ ban リストを実装。

### 3.5 Mix ローテーション

```ts
interface PoolConfig {
  rotation: VariantId[];        // 例: ['nlh','plo','27td','razz','stud8']
  rotationPeriod: { kind: 'hands'; count: number } | { kind: 'minutes'; count: number };
}
```

**プールは1つだけ。種目がプール全体で同時に切り替わる。** ゲームごとにプールを分けると
各プールの人数が足りず、ファストフォールドが成立しません（これが最大の設計判断です）。
UI には「現在: PLO / 次: 2-7TD（あと8ハンド）」を常時表示。

### ~~3.6 プロトコル~~ → 実装済（§8）

- WebSocket。サーバー→クライアントのイベントは**席ごとに個別生成・個別配信**。ブロードキャストで全員に同じペイロードを送る実装は禁止（ホールカードが漏れる典型パターン）。
- クライアント→サーバーのアクションは `{ handId, actionSeq, action }`。`actionSeq` 不一致は無視。これでダブルクリックと遅延クリックの誤操作が消えます。
- タイムバンク: 1アクション15秒 + 累積バンク。切断時は自動チェック/フォールド。

### 3.7 永続化

- 卓の進行中状態は**インメモリのみ**。DB に毎アクション書くと死にます。
- Postgres に書くのは**ハンド終了時に1レコード**: ハンド履歴（イベントログJSONB）、各席の収支、オールインEV。
- スタッツ（VPIP/PFR/3B等）はハンド履歴から日次バッチで集計。リアルタイム集計は不要。

---

## 3.9 WSOP ルールとの対応

実装済み（WSOP 準拠）:
- ミニマムレイズは直前のベット/レイズ幅以上
- フルレイズに満たないオールインはアクションを再オープンしない
- コールされなかったベットは返却
- チョップの端数はボタンの左から最初の勝者へ
- ハイローの端数はハイ側へ
- ヘッズアップはボタンが SB を出し、プリフロップ先行・ポストフロップ後手

**意図的な差分**:
- FL の4ベットキャップをヘッズアップでも外さない（上記）

未実装（online では通常問題にならないが、把握しておくもの）:
- ショーダウン順（最後のアグレッサーから開示、ベットが無ければボタンの左から）。現状は到達者を同時開示
- 負けハンドのマック。現状はショーダウン到達者を全公開
- デッドボタン。M3 でテーブルが毎ハンド組み直されるため該当しない
- ミスディール、エクスポーズドカード、ラビットハント、ラン・イット・トワイス

---

## 4. 不変条件（テストで守るもの）

1. **チップ保存則**: 1ハンドの支払い合計 = 徴収合計。
2. **デッキ整合性**: 1ハンド内で同じカードが2回出現しない。配札のたびに assert。
3. **情報漏洩なし**: 各席に送ったイベントを全部集めても、他席のホールカードがショーダウン前に含まれない。これは**テストで自動検証**してください。
4. **乱数**: `crypto.randomInt` のみ。`Math.random` はテスト専用。
5. **リプレイ決定性**: ハンド履歴 + シードから再実行して同一結果。

推奨: 各 stable バリアントについて、ランダムアクションで10万ハンド回して1〜4を assert するプロパティテスト。ここで落ちるバグが本番で一番痛いものです。

---

## 5. ルール未確定のもの（ここを埋めないと実装できません）

| ゲーム | 状態 | 決めるべきこと |
|---|---|---|
| Badacey / Baducey | draft | 3枚ハンドでもバドゥーギ側を取れるか |
| Dramaha / Dramadugi | draft | ドローの回数とタイミング、ドロー側に最低役を要求するか |
| **Archie** | **未設定** | 流通しているルールが互換でない。ハイ側/ロー側のクオリファイア、ドロー式かフロップ式か。**まずルール文書を書く** |

`registry.ts` の `status: 'draft'` は本番に出す前にハウスルール文書の承認が必要、という意味です。
AIに「Archieを実装して」と投げると、それらしいが誰のルールでもないものが出てきます。

---

## 6. マイルストーン

1. **M0（完了）**: 評価器 + バリアントスキーマ + ショーダウン
2. **M1（完了）**: ベッティングステートマシン + ポットマネージャ + NLH 1卓が完走、リプレイ一致
3. **M2（完了）**: WebSocket + 席ごと配信 + React テーブルUI、NLH を人間6人で遊べる
4. **M3**: ファストフォールド・プール（NLH単独）、100bbリセット、公開
5. **M4（完了）**: PLO / PLO8 / BigO / FLH / FLO8。設定追加のみで通り、エンジン本体の変更は端数ルールの1箇所だけ
6. **M5**: Mix ローテーション有効化
7. **M6**: ドロー系（2-7TD → A-5TD → Badugi → NL2-7SD）
8. **M7**: スタッド系（Razz → 7Stud → Stud8）
9. **M8**: draft ゲーム（ハウスルール確定後）

M3 までは**1ゲームだけ**で進めてください。Mix は M5 で初めて有効化します。エンジンは最初から全ゲーム対応で作るが、**公開するのは1種目から**。ここを混同すると M1 で永遠に足踏みします。

---

## 7. 法務メモ（弁護士の助言ではありません）

入金・換金・チップ購入・景品交換を一切持たない構成を維持すること。リーダーボードの賞品に金銭的価値を持たせると前提が崩れます。公開前に利用規約とプライバシーポリシーを用意し、トップページに「賭博行為は行えません」を明示（参照2サイトが揃ってそうしているのは理由があります）。


---

## 8. M2: 配信と情報漏洩対策

### 絶対規則

**`HandState` はサーバープロセスから出ない。** メッセージにも、クライアントが読めるログにも、「デバッグ用に一時的に」も無し。ワイヤーに乗るのは `HandView` だけで、`HandView` は**1人の viewer 専用**に構築されます。

見えないカードは `HandView` 上で `null` です。**このファイルの型には他人のホールカードを載せられる形が存在しません**。漏らすには新しいメッセージ型を発明する必要があり、それはレビューで止まる変更です。

```
packages/protocol/src/index.ts
  redactHand(state, variant, handId, viewerSeat, names, seq) → HandView
  assertNoLeak(view, state)     // 開発時に全送信で実行
  parseClientMessage(raw)       // 受信は必ずここを通す
```

`Table.broadcast()` は接続中のプレイヤーごとに `redactHand` を呼び直します。**「再計算を避けるために」ループの外に出した瞬間に全員のホールカードが全員に飛びます。** そのためのコメントがコードに書いてあります。消さないでください。

### 公開条件

ショーダウンに到達した席のみ公開。**全員フォールドで決着したポットは何も公開しません**。フォールドを「オープンして捨てる」実装にすると、ここが崩れます。

### 検証

- `apps/server/tests/table.test.ts`: 300ハンドを回し、全プレイヤーの全ビューを走査して他席のカードが `null` 以外になっていないことを検査。ショーダウン後のみ公開を許可。テストが空振りしないよう「実際に公開が発生したこと」も assert
- `apps/server/tests/ws.test.ts`: **実ソケット**での検証。Alice が自分のカードを知っている状態で、Bob が受信した生フレーム全文を走査し、そのカード値が一度も現れないことを確認。`"deck"` `"burned"` が1バイトも乗らないことも確認

### アクションの検証

`{ handId, actionSeq, action }` の3点照合。`actionSeq` はビューに含まれ、クライアントはそれをエコーします。ダブルクリックと、状態が進んだ後に着弾したクリックは `stale action` で弾かれ、**テーブルには一切影響しません**。不正なアクションは送信者にだけエラーが返り、他席には何も起きません。

### タイムアウト

`Table.tick()` をトランスポートが定期的に呼びます。チェックできるならチェック、できなければフォールド。時刻は注入なので、テストは時計を進めるだけで検証できます。

### 認証（実装済み）

`apps/server/src/auth.ts` の `TokenVerifier` だけが `playerId` の発生源です。**クライアントのメッセージ内の値が identity になることはありません。** `playerId` や `uid` のようなフィールドを受信メッセージに足して使った時点で、このファイルは無意味になります。

- `AUTH_MODE=firebase` → Firebase ID トークンを `verifyIdToken(token, true)` で検証。第2引数を落とさないこと。落とすと、ユーザーが全端末からサインアウトした後も盗まれたトークンが通り続けます
- それ以外 → dev verifier（トークン＝uid）。`NODE_ENV=production` では**起動時に例外を投げて停止**します
- トークン失効と偽造をクライアントに区別させない。どちらも「サインインし直してください」

接続後の最初のメッセージは `auth` でなければならず、成功するまで他のメッセージは一切処理しません。検証は非同期なので、1接続内のメッセージは**キューで順序保証**しています。これが無いと `auth` の直後に送られた `join-room` が検証完了前に走ります。

1アカウント1ソケット。2枚目の窓を開くと古い方を切ります（同一テーブルに2席持つのを防ぐため）。

Firebase ID トークンの寿命は約1時間なので、クライアントは30分ごとに再送します。同一 uid での再 `auth` はセッションを保ったままです。

**Firebase Auth が保証しないもの**: 多重アカウント（1人が10アカウント作るのは自由）、Bot、共謀。リーダーボードをやるならこの3つは別途対策が要ります。

---

## 9. プライベートルーム（参加コード）

`apps/server/src/rooms.ts`。`RoomManager` が複数の `Table` をコードで束ねます。

- コードは6文字、`23456789ABCDEFGHJKLMNPQRSTUVWXYZ`（0/O、1/I/L を除外）。口頭で伝えて電話で打つものなので、打ち間違いは「友人が入れない」と同義です。32^6 ≈ 10億
- 存在しないコードと満室で**同じエラー文**を返す。区別するとコードの存在確認に使われます
- 総当たり対策として1人あたり60秒で10回まで
- ホストが抜けてもルームは閉じません。1人の電源が切れて全員が蹴られるのは事故です
- 空になってから10分で破棄。リロードで戻ってこられます
- バイインはサーバー側固定（`buyInBb: 100`）。クライアントが金額を指定できる設計にしないこと

**ルーム間の分離**はテストで固定しています（`apps/server/tests/rooms.test.ts`）。注意点として、**カードの値で分離は証明できません**。別ルームは独立したデッキを切るので同じ値が正当に両方へ出ます。証明すべきは identity の方（handId、ルームコード、参加者）です。

`handId` はランダムサフィックス付きです。タイムスタンプ由来にすると、2つのルームが同じミリ秒で最初のハンドを開始したときに衝突します（テストで実際に出ました）。ハンド履歴の主キーになるので、ここは一意性が必須です。

**リーダーボードを作るときは、プライベートルームの結果を必ず除外してください。** 友人同士で結果を作り合えるので、集計に入れた瞬間に意味がなくなります。


---

## 10. セッション収支（友人戦のスコア）

`apps/server/src/scoreboard.ts`。ルームごとに1つ。

各ハンド終了時に `Table` が「そのハンドの各プレイヤーの増減」を渡し、Scoreboard が累計します。`points` = セッション通算の純増減。

### ゼロサムを「信じずに検算する」

エンジンは既にハンド内のチップ保存則を assert しています。Scoreboard は**それとは別の層で、プレイヤーが実際に見る数字に対して**同じ性質を再計算します。二重化する理由は、プレイヤーが気にする不具合が「友達の点が増えたのに誰も減っていない」という**表示側のバグ**であって、ルール側のバグではないからです。

- 合計が0でないハンドを検出したら `balanced` を false にし、**以後ずっと false のまま**、UIにも表示します
- 静かに辻褄を合わせる実装にしないでください。隠すと、まさに知りたいことが消えます
- テストでは PLO8 の乱択300ハンドを回し、**毎ハンド直後に合計が0であること**を確認しています。加えて「全員0点なら合計0は自明に成立してしまう」ので、実際に収支が動いていることも assert しています

### 途中参加

`ensure()` で0点から始まります。毎ハンド100bbリセットなので、**誰がいつ入っても合計は常に0**です。

**もしスタックを持ち越す通常のキャッシュゲームに変えるなら、この性質は壊れます。** 途中リバイを収支から差し引く処理が別途必要になります。現状の「毎ハンドリセット」は、単に fast-fold 向けの設計であるだけでなく、**収支が監査可能である理由**でもあります。

### 履歴

直近200ハンド分の累計推移をグラフ用に保持。メモリは人数×200で有界。`hands` カウント自体は打ち切りません。永続化はハンド履歴（M4）と一緒にやるべきで、現状は**サーバー再起動で消えます**。
