import { createReadStream, existsSync, statSync } from 'node:fs';
import { createServer, type Server } from 'node:http';
import { extname, join, normalize, resolve } from 'node:path';
const MIME: Record<string,string>={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.jpg':'image/jpeg','.webmanifest':'application/manifest+json','.ico':'image/x-icon','.woff2':'font/woff2'};
export function createHttpServer(staticDir:string|null):Server{const root=staticDir?resolve(staticDir):null;return createServer((req,res)=>{if(req.method!=='GET'&&req.method!=='HEAD'){res.writeHead(405).end();return;}const url=new URL(req.url??'/','http://localhost');if(url.pathname==='/healthz'){res.writeHead(200,{'content-type':'text/plain'}).end('ok');return;}if(!root||!existsSync(root)){res.writeHead(200,{'content-type':'text/plain; charset=utf-8'}).end('Mixed Nuts server');return;}const requested=resolve(join(root,normalize(decodeURIComponent(url.pathname))));const isInside=requested===root||requested.startsWith(root+'/');const file=isInside&&existsSync(requested)&&statSync(requested).isFile()?requested:join(root,'index.html');if(!existsSync(file)){res.writeHead(404).end();return;}const type=MIME[extname(file)]??'application/octet-stream';const immutable=file.includes('/assets/');res.writeHead(200,{'content-type':type,'cache-control':immutable?'public, max-age=31536000, immutable':'no-cache'});if(req.method==='HEAD'){res.end();return;}createReadStream(file).pipe(res);});}
